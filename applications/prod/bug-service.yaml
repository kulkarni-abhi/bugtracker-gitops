apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bug-service-prod
  namespace: argocd
spec:
  project: default

  sources:
    - repoURL: https://github.com/kulkarni-abhi/bugtracker-bug-service
      targetRevision: dev
      path: helm
      helm:
        valueFiles:
          - $values/environments/prod/bug-service-values.yaml

    - repoURL: https://github.com/kulkarni-abhi/bugtracker-gitops
      targetRevision: main
      ref: values

  destination:
    server: https://kubernetes.default.svc
    namespace: prod

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
