apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: user-service-dev
  namespace: argocd
spec:
  project: default

  sources:
    - repoURL: oci://ghcr.io/kulkarni-abhi/helm
      chart: bugtracker-user-service
      targetRevision: 0.2.0
      helm:
        valueFiles:
          - $values/environments/dev/user-service-values.yaml

    - repoURL: https://github.com/kulkarni-abhi/bugtracker-gitops
      targetRevision: main
      ref: values

  destination:
    server: https://kubernetes.default.svc
    namespace: dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
