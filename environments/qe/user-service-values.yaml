replicaCount: 1

image:
  repository: ghcr.io/kulkarni-abhi/bugtracker-user-service
  tag: "CHANGE_ME"
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80

containerPort: 8080

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 200m
    memory: 256Mi
